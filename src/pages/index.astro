---

---

<html lang="cs">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />
		<title>Natios Health</title>
	</head>
	<body>
		<main>
			<h1>Natios – It's better to be the best</h1>
			<img
				src="https://cdn.myshoptet.com/usr/740472.myshoptet.com/user/documents/upload/assets/logo_natios_bez_pozadi.svg"
				alt="Natios Logo"
				class="logo"
			/>
			<div class="contact-info">
				<p><strong>Natios Health, s.r.o.</strong></p>
				<p>Nádražní 801/99, 702 00 Ostrava, Czech Republic</p>
				<p>Company ID (IČO): 28582292</p>
				<p>VAT ID (DIČ): CZ28582292</p>
				<p>Case file number: C 33027, maintained by the Regional Court in Ostrava, Czech Republic</p>
				<p>+420 790 388 488</p>
				<p><a href="mailto:info@natios.cz">info@natios.cz</a></p>
			</div>
		</main>
	</body>
</html>

<style>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	body {
		min-height: 100vh;
		display: flex;
		justify-content: center;
		align-items: center;
		font-family: "Poppins", sans-serif;
		color: white;
		overflow: hidden;

		background: linear-gradient(135deg, #004cb8, #051461, #003a8c, #051461, #004cb8);
		background-size: 300% 300%;
		animation: gradientFloat 25s ease-in-out infinite;
	}

	/* Soft light depth */
	body::before {
		content: "";
		position: absolute;
		inset: 0;
		background: radial-gradient(circle at 50% 40%, rgba(255, 255, 255, 0.08), transparent 60%);
		animation: lightDrift 30s ease-in-out infinite;
		pointer-events: none;
	}

	/* Very subtle grain (optional but recommended) */
	body::after {
		content: "";
		position: absolute;
		inset: 0;
		background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.7' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
		pointer-events: none;
	}

	/* MAIN MOTION – no rotation, no scaling */
	@keyframes gradientFloat {
		0% {
			background-position: 0% 50%;
		}
		50% {
			background-position: 100% 50%;
		}
		100% {
			background-position: 0% 50%;
		}
	}

	/* SOFT LIGHT DRIFT */
	@keyframes lightDrift {
		0% {
			background-position: 40% 40%;
		}
		50% {
			background-position: 60% 55%;
		}
		100% {
			background-position: 40% 40%;
		}
	}

	main {
		text-align: center;
		padding: 3rem;
		max-width: 800px;
	}

	h1 {
		font-size: 2.5rem;
		margin-bottom: 3rem;
		line-height: 1.2;
		font-weight: 600;
	}

	.logo {
		width: 300px;
		max-width: 100%;
		height: auto;
		margin-bottom: 4rem;
	}

	.contact-info {
		line-height: 1.8;
	}

	.contact-info p {
		margin-bottom: 0.5rem;
	}

	.contact-info a {
		color: white;
		text-decoration: none;
	}

	.contact-info a:hover {
		text-decoration: underline;
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 1.8rem;
		}

		.logo {
			width: 200px;
		}

		main {
			padding: 1rem;
		}
	}
</style>
